import { moduleFor, test } from 'ember-qunit';

moduleFor('view:authentication/signup', 'Unit | View | authentication/signup');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
